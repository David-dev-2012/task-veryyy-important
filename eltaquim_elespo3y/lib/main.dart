import 'package:flutter/material.dart';

import 'app/air_sprint_app.dart';
//the first step to make flutter project
void main () {
  runApp(const AirSprintApp());
  //const used to add var in compile time
}